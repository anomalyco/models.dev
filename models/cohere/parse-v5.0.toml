# Sources:
# https://docs.cohere.com/docs/models
# https://docs.cohere.com/docs/parse
# https://docs.cohere.com/v2/reference/parse
#
# release_date/last_updated: Cohere docs list parse-v5.0 as Live but do not
# publish a launch day. Values below are the catalog verification date
# (2026-08-27), not a first-party launch announcement — correct if a
# definitive public launch date is found.
name = "Parse v5.0"
description = "Vision parsing model that extracts structured Markdown or blocks from document images"
release_date = "2026-08-27"
last_updated = "2026-08-27"
attachment = true
reasoning = false
temperature = false
tool_call = false
open_weights = false

[limit]
context = 8_192
output = 8_192

[modalities]
input = ["image"]
output = ["text"]
