# Effort: reasoning_effort = none|low|medium|high (Zen free tier rejects probes outside
# OpenCode — 403 FreeTierError — so values come from the live zen/go probe on the same
# base_model 2026-09-22 plus kilo's effort entry for xiaomi/mimo-v2.6-*; none ∈ effort so
# no separate toggle)
# https://mimo.mi.com/docs/api/chat/openai-api (accessed 2026-09-22)
base_model = "xiaomi/mimo-v2.6-flash"
name = "MiMo-V2.6-Flash Free"
reasoning_options = [{ type = "effort", values = ["none", "low", "medium", "high"] }]

[interleaved]
field = "reasoning_content"

[cost]
input = 0
output = 0
cache_read = 0

[limit]
context = 200_000
output = 32_000
