# CNY→USD rate: 6.718405, 2026-09-20, source: https://open.er-api.com/v6/latest/USD
# CNY list price source: https://ppio.com/pricing
# Tiered pricing: PPIO splits the 1-32000 context band by output length
# (up to 200 output tokens at CNY 2/8, above at CNY 3/14). models.dev tiers
# are context-only, so the general-case rate is used for that band.
# cache_write is published only for the 32000+ band (CNY 0.8); PPIO lists no
# cache_write rate for the 1-32000 band.
# Toggle: thinking.type = enabled|disabled
base_model = "zhipuai/glm-4.7"
structured_output = true
reasoning_options = [{ type = "toggle" }]

[interleaved]
field = "reasoning_content"

[cost]
input = 0.44653
output = 2.08383
cache_read = 0.08931

[[cost.tiers]]
tier = { type = "context", size = 32_000 }
input = 0.59538
output = 2.38152
cache_read = 0.11908
cache_write = 0.11908
