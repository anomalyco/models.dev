# Reasoning HTTP format (source accessed 2026-07-15): disable thinking by
# extending the request body with `{"chat_template_kwargs": {"thinking": false}}`.
# Thinking is on by default; there is no documented effort/budget control, only
# this on/off switch.
# https://docs.privatemode.ai/models/overview/
base_model = "moonshotai/kimi-k2.6"
reasoning_options = [{ type = "toggle" }]

[cost]
input = 0.00
output = 0.00

[limit]
output = 32_768

[modalities]
input = ["text", "image"]
output = ["text"]
