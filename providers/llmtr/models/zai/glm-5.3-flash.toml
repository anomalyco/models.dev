# Generated by `bun llmtr:generate` from https://llmtr.com/api/models (accessed 2026-08-29).
# reasoning_options reflect LLMTR's own chat surface, not the base model's flag.
# Effort: $.reasoning_effort = low|high|max (model suffixes :low|:high|:max are aliases).
base_model = "zhipuai/glm-5.3-flash"

[cost]
input = 0.075
output = 0.25

[limit]
context = 1_000_000

[[reasoning_options]]
type = "effort"
values = ["low", "high", "max"]
