# Hand-authored from LLMTR's public catalog https://llmtr.com/api/models (accessed 2026-08-16).
# reasoning_options measured against POST https://llmtr.com/v1/chat/completions (2026-08-16).
# Toggle: $.enable_thinking = true|false (Alibaba native field, forwarded by the gateway).
# Measured: thinking is on by default (~779-942 reasoning tokens) and enable_thinking=false switches it off (reproduced). $.reasoning, :think/:fast and reasoning_effort are inert or rejected here.
base_model = "alibaba/qwen3.5-397b-a17b"

[cost]
input = 0.6
output = 3.6

[limit]
context = 256_000

[[reasoning_options]]
type = "toggle"
