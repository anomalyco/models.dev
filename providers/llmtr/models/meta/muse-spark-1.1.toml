# Generated by `bun llmtr:generate` from https://llmtr.com/api/models (accessed 2026-08-29).
# reasoning_options reflect LLMTR's per-model capabilities, not the base model's flag.
base_model = "meta/muse-spark-1.1"

[cost]
input = 1.25
output = 4.25

[limit]
context = 1_048_576

[[reasoning_options]]
# API: $.reasoning.effort = minimal|low|medium|high|xhigh (model suffixes :minimal|:low|:medium|:high|:xhigh are aliases)
type = "effort"
values = ["minimal", "low", "medium", "high", "xhigh"]
