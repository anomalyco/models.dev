# Generated by `bun llmtr:generate` from https://llmtr.com/api/models (accessed 2026-08-29).
# reasoning_options reflect LLMTR's own chat surface, not the base model's flag.
# Effort: $.reasoning_effort = none|minimal|low|medium|high|xhigh|max (model suffixes :none|:minimal|:low|:medium|:high|:xhigh|:max are aliases).
base_model = "meta/muse-glimmer-30b"

[cost]
input = 0.35
output = 1.5

[limit]
context = 131_072

[[reasoning_options]]
type = "effort"
values = ["none", "minimal", "low", "medium", "high", "xhigh", "max"]
