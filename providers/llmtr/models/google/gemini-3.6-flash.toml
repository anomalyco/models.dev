# Generated by `bun llmtr:generate` from https://llmtr.com/api/models (accessed 2026-08-29).
# reasoning_options reflect LLMTR's own chat surface, not the base model's flag.
# Effort: $.reasoning_effort = minimal|low|medium|high (model suffixes :minimal|:low|:medium|:high are aliases).
base_model = "google/gemini-3.6-flash"

[cost]
input = 1.5
output = 7.5

[limit]
context = 1_048_576

[[reasoning_options]]
type = "effort"
values = ["minimal", "low", "medium", "high"]
