# Generated by `bun llmtr:generate` from https://llmtr.com/api/models (accessed 2026-08-29).
# reasoning_options reflect LLMTR's per-model capabilities, not the base model's flag.
base_model = "anthropic/claude-opus-5"

[cost]
input = 5
output = 25

[limit]
context = 1_000_000

[[reasoning_options]]
# API: $.reasoning.effort = low|medium|high|xhigh|max (model suffixes :low|:medium|:high|:xhigh|:max are aliases)
type = "effort"
values = ["low", "medium", "high", "xhigh", "max"]
