# Generated by `bun llmtr:generate` from https://llmtr.com/api/models (accessed 2026-08-29).
# reasoning_options reflect LLMTR's own chat surface, not the base model's flag.
# Effort: $.reasoning_effort = low|medium|high|xhigh (model suffixes :low|:medium|:high|:xhigh are aliases).
base_model = "anthropic/claude-opus-4-7"

[cost]
input = 5
output = 25

[limit]
context = 1_000_000

[[reasoning_options]]
type = "effort"
values = ["low", "medium", "high", "xhigh"]
