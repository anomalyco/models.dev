# Generated by `bun llmtr:generate` from https://llmtr.com/api/models (accessed 2026-08-29).
# reasoning_options reflect LLMTR's own chat surface, not the base model's flag.
# Effort: $.reasoning_effort = low|medium|high (model suffixes :low|:medium|:high are aliases).
base_model = "stepfun/step-3.7-flash"

[cost]
input = 0.2
output = 1.15

[limit]
context = 256_000

[[reasoning_options]]
type = "effort"
values = ["low", "medium", "high"]
