# Generated by `bun llmtr:generate` from https://llmtr.com/api/models (accessed 2026-08-29).
# reasoning_options reflect LLMTR's own chat surface, not the base model's flag.
# Measured 2026-08-29 (probed step-3.5-flash): reasoning_effort is rejected 400 "does not expose a
# thinking mode", and thinking.type / enable_thinking / thinking_budget / reasoning leave
# reasoning_content unchanged. No caller-facing control on this route.
# The sibling step-3.5-flash-2603 route does advertise reasoning_effort low|high and accepts it,
# so the empty surface here is this id's, not the family's.
base_model = "stepfun/step-3.5-flash"
reasoning_options = []

[cost]
input = 0.1
output = 0.3

[limit]
context = 256_000
