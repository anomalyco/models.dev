# Generated by `bun llmtr:generate` from https://llmtr.com/api/models (accessed 2026-08-29).
# reasoning_options reflect LLMTR's own chat surface, not the base model's flag.
# Effort: $.reasoning_effort = low|high (model suffixes :low|:high are aliases).
base_model = "stepfun/step-3.5-flash-2603"

[cost]
input = 0.1
output = 0.3

[limit]
context = 256_000

[[reasoning_options]]
type = "effort"
values = ["low", "high"]
