# Generated by `bun llmtr:generate` from https://llmtr.com/api/models (accessed 2026-08-29).
# reasoning_options reflect LLMTR's per-model capabilities, not the base model's flag.
base_model = "tencent/hy4-preview"

[cost]
input = 0.834
output = 2.501

[limit]
context = 262_144

[[reasoning_options]]
# API: $.reasoning.effort = none (model suffixes :none are aliases)
type = "effort"
values = ["none"]
