# Serving clamps larger output requests to 15000; public catalog lists 262144.
# See ../README.md for operator evidence and public API probe records.
# Default: thinking enabled; reasoning_effort=none switches it off.
# Live reasoning probe: none -> no reasoning; low -> reasoning_content (2026-09-22).
# Direct API catalog and USD pricing (2026-09-22):
# https://inference.api.reka.ai/v1/site/models?capability=chat
# Effort: reasoning_effort = none|low|high|max; none disables thinking.
base_model = "zhipuai/glm-5.3-flash"
reasoning_options = [{ type = "effort", values = ["none", "low", "high", "max"] }]

[interleaved]
field = "reasoning_content"

[cost]
input = 0.15
output = 0.5

[limit]
context = 262144
output = 15000

[modalities]
input = ["text", "image"]
