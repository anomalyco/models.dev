# Merge Gateway GET /v1/models?model=anthropic/claude-fable-5 (accessed 2026-07-14):
# the only available Anthropic route reports `supports_reasoning = false`, so this
# provider route does not expose the base model's reasoning capability.
base_model = "anthropic/claude-fable-5"
reasoning = false
structured_output = true

[cost]
input = 10
output = 50
cache_read = 1
cache_write = 12.5
