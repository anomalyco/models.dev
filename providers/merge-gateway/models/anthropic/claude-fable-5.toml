# Merge Gateway GET /v1/models?model=anthropic/claude-fable-5 (accessed 2026-07-16):
# the route reports `supports_reasoning = false`, but that field is unreliable as a
# negative signal (the same Anthropic models report `true` via bedrock routes), so
# reasoning is inherited from base metadata with no verified gateway control.
base_model = "anthropic/claude-fable-5"
reasoning_options = []
structured_output = true

[cost]
input = 10
output = 50
cache_read = 1
cache_write = 12.5
