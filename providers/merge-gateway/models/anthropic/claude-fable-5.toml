# Merge Gateway GET /v1/models?model=anthropic/claude-fable-5 (accessed 2026-07-14):
# the only available Anthropic route reports `supports_reasoning = false`, so
# callers have no reasoning control despite the base model being reasoning-capable.
base_model = "anthropic/claude-fable-5"
structured_output = true
reasoning_options = []

[cost]
input = 10
output = 50
cache_read = 1
cache_write = 12.5
