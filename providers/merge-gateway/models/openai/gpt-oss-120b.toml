# Merge Gateway GET /v1/models?model=openai/gpt-oss-120b (accessed 2026-07-14):
# available routes either report `supports_reasoning = false` or reasoning with
# `configurable = false` and no controls, so callers have no reasoning control.
base_model = "openai/gpt-oss-120b"
reasoning = false
tool_call = false
structured_output = false

[cost]
input = 0.09
output = 0.36
