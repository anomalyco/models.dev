# Merge Gateway GET /v1/models?model=openai/gpt-oss-20b (accessed 2026-07-14):
# available routes either report `supports_reasoning = false` or reasoning with
# `configurable = false` and no controls, so callers have no reasoning control.
base_model = "openai/gpt-oss-20b"
reasoning_options = []

[cost]
input = 0.07
output = 0.3

[limit]
context = 128_000
output = 128_000
