# Cost: Melious list price for the bare model ID -- the default `balanced` route
# -- EUR 0.60 in / EUR 0.65 out per 1M tokens, converted at 1.1592 USD/EUR (ECB
# euro reference rate, 2026-09-11). The hub's per-flavour panel quotes EUR 0.65
# in / EUR 0.65 out for `balanced`; the models endpoint above is what the bare
# ID is billed at, so that is what is published here.
# Sources: GET https://api.melious.ai/v1/models?include_meta=true and
# https://melious.ai/hub/models/llama-3.3-70b-instruct (both accessed 2026-09-15)
# FX: https://www.ecb.europa.eu/stats/policy_and_exchange_rates/euro_reference_exchange_rates/html/eurofxref-graph-usd.en.html
# limit.context: this deployment serves 131_072 tokens, not the lab's 128_000.

base_model = "meta/llama-3.3-70b-instruct"

[cost]
input = 0.69552
output = 0.75348

[limit]
context = 131_072
