# Fast alias of qwen3.6-35b with thinking pinned off through the chat
# template: every reasoning_effort value collapses to none and cannot be
# overridden (capabilities.reasoning = false, supported_efforts ["none"]),
# per GET /v1/models and the chat-completions per-model table (checked
# 2026-08-27). Pricing from metadata.pricing (cached input at the 10%
# platform default).
# https://portal.neuralwatt.com/docs/api/models
# https://portal.neuralwatt.com/docs/api/chat-completions
base_model = "alibaba/qwen3.6-35b-a3b"
name = "Qwen3.6 35B Fast"
reasoning = false

[cost]
input = 0.29
output = 1.15
cache_read = 0.029

[limit]
context = 131_056
output = 131_056

[modalities]
input = ["text", "image"]
output = ["text"]
