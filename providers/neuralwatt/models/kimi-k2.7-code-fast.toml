# Fast alias of kimi-k2.7-code: same model, pricing, and reasoning contract
# per GET /v1/models (checked 2026-08-27) — no published effort ladder
# (metadata.reasoning null, capabilities.reasoning_effort false), treated as
# reasoning-always-on with no dial. Pricing identical to the standard alias.
# https://portal.neuralwatt.com/docs/api/models
# https://portal.neuralwatt.com/docs/api/chat-completions
base_model = "moonshotai/kimi-k2.7-code"
name = "Kimi K2.7 Code Fast"

reasoning_options = []

[cost]
input = 0.95
output = 4.0
cache_read = 0.095

[limit]
context = 262_128

[modalities]
input = ["text", "image"]
output = ["text"]
