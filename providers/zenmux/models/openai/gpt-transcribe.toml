# ZenMux bills this audio route at USD 0.000075 per second, not per token;
# the token-only cost schema cannot represent it, so cost is intentionally omitted.
# https://zenmux.ai/docs/api/openai/openai-list-models.html
base_model = "openai/gpt-transcribe"
