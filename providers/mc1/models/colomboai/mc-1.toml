name = "MC-1 — Auto Intelligence"
description = "ColomboAI's logical intelligence model that routes each request to an eligible executable model under project policy."
family = "model-router"
release_date = "2026-08-15"
last_updated = "2026-09-02"
attachment = false
reasoning = false
temperature = true
tool_call = true
structured_output = true
open_weights = false

# MC-1 uses dynamic route pricing rather than one fixed per-token model price.
[limit]
context = 128000
output = 4096

[modalities]
input = ["text"]
output = ["text"]
