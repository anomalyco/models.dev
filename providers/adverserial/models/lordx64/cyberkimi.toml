# REVIEW DRAFT ONLY: required unresolved fields are intentionally omitted.
# Sources: https://adverserial.ai/ and https://adverserial.ai/docs.html
name = "CyberKimi"
description = "Cybersecurity-focused model served by Adverserial AI."
attachment = false
reasoning = true
tool_call = true
structured_output = false
# Pending: release_date, last_updated, open_weights, reasoning_options.
# Reasoning controls must be verified on the public Chat Completions endpoint.

[cost]
input = 8.00
output = 30.00
cache_read = 0.80

[limit]
context = 1048576
# Pending: output. 65536 is a gateway default, not a verified maximum.

[modalities]
input = ["text"]
output = ["text"]
