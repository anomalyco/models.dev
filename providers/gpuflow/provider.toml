name = "GPU Flow"
env = ["GPUFLOW_API_KEY"]
npm = "@ai-sdk/openai-compatible"
# Raw HTTP is POST /v1/chat/completions (OpenAI-compatible) at
# https://api.gpuflow.ai/v1. Open-weight models served on NVIDIA B200
# in Madrid (es-mad-1). Catalog and pricing:
# https://api.gpuflow.ai/v1/models (accessed 2026-09-23)
api = "https://api.gpuflow.ai/v1"
doc = "https://gpuflow.ai/es/docs"
