# Toggle: enable_thinking = true|false (chat_template_kwargs passthrough).
# NVIDIA Nemotron 3.5 Lightning 30B-A3B MoE (~3B active, Mamba-2 hybrid)
# in NVFP4 on a MIG-sliced B200 in Madrid. Tool calling verified
# empirically against the engine (qwen3_coder parser, 2026-09-23).
# json_mode is not published on this host yet.
# https://api.gpuflow.ai/v1/models (accessed 2026-09-23)
base_model = "nvidia/nemotron-3.5-lightning"
name = "Nemotron 3.5 Lightning 30B A3B"
reasoning_options = [{ type = "toggle" }]
structured_output = false

# The serving stack emits interleaved reasoning in the `reasoning` field
# (not `reasoning_content`), which the current interleaved.field enum cannot
# express, so no interleaved side channel is declared here.

[cost]
input = 0.12
output = 0.3

[limit]
context = 131_072
output = 16_384
