base_model = "meta/muse-glimmer-30b"

# Efforts per fireworks peer (models without API-exposed levels keep lab controls)
[[reasoning_options]]
type = "effort"
values = ["low", "medium", "high", "xhigh"]

[cost]
input = 0.7
output = 3.5
cache_read = 0.35
