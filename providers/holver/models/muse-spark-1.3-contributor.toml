# Served by Holver (https://api.holver.id/v1, OpenAI-compatible,
# Bearer $HOLVER_API_KEY). The Contributor tier is Holver's serving of Muse
# Spark 1.3; effort levels mirror Meta's first-party Contributor entry
# (no "max"). Catalog: https://holver.id/models. Holver bills via credit
# packages, not per-model USD rates, so cost is intentionally omitted.
# Reasoning on this host: Holver forwards only the OpenAI-style top-level
# `reasoning_effort` field upstream. Holver does not interpret it — it is
# passed through to the routed Muse Spark endpoint.
base_model = "meta/muse-spark-1.3"
name = "Muse Spark 1.3 Contributor"
reasoning_options = [{ type = "effort", values = ["minimal", "low", "medium", "high", "xhigh"] }]
