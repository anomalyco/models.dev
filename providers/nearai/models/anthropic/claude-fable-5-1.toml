# No caller-side reasoning control on this route. Verified live on
# POST https://cloud-api.near.ai/v1/chat/completions 2026-09-08:
# thinking.type = "enabled" -> `"thinking.type.enabled" is not supported for this model`
# reasoning_effort -> `reasoning_effort: Extra inputs are not permitted`
# reasoning.enabled / .effort / .max_tokens -> accepted but no reasoning_content returned
base_model = "anthropic/claude-fable-5-1"
reasoning_options = []

[cost]
input = 10
output = 50
cache_read = 0.25

[modalities]
input = ["text", "image"]
