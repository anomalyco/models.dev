# Reasoning: none forwarded. This host rebuilds each deepseek request from a
# fixed parameter map and sends nothing else: frequency_penalty, logprobs,
# max_completion_tokens, max_tokens, messages, model, parallel_tool_calls,
# presence_penalty, response_format, stop, stream, stream_options, temperature,
# tool_choice, tools, top_logprobs, top_p. It has no reasoning_effort and no
# thinking field.
# https://platform.hopscotchlabs.ai/models
base_model = "deepseek/deepseek-v4-pro"
reasoning_options = []

[cost]
input = 0.435
output = 0.87
cache_read = 0.044
