# Model ID cline-pass/mimo-v2.6-pro; reference pricing from the Cline model
# catalog https://api.cline.bot/api/v1/ai/cline/models (accessed 2026-09-22):
# $0.435 / $0.87 / $0.0036 per 1M tokens (input/output/cache read).
# Wire evidence (2026-09-22): POST /api/v1/chat/completions with
# reasoning_effort = none|low|medium|high|xhigh|minimal|max returns HTTP 200
# on this host, so these seven effort levels are exposed. Xiaomi first-party
# API is thinking.type = enabled|disabled; this relay adds the effort scale.
base_model = "xiaomi/mimo-v2.6-pro"
reasoning_options = [{ type = "effort", values = ["none", "minimal", "low", "medium", "high", "xhigh", "max"] }]

[cost]
input = 0.435
output = 0.87
cache_read = 0.0036
