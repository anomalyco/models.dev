# release_date is the /v1/models `created` timestamp.
# Pricing: https://docs.lightcone.ai/guides/pricing/ lists `tzafon.northstar-cua-fast-1.6`
# at $0.50/M input as of 2026-07-12. The page's pricing table only shows one price per
# model (its Input/Output columns collapse into a single value); output isn't broken out
# for fast-1.6 there, so cost.output is assumed equal to tzafon.northstar-cua-fast's
# $1.50/M (the only output figure the page states, in its northstar-cua-fast callout) —
# confirm/update once Tzafon publishes fast-1.6's output price explicitly.
# Limits probed 2026-07-02 via an oversized max_completion_tokens request:
# max_model_len=max_total_tokens=131072 (shared input+output budget).

name = "Northstar CUA Fast 1.6"
description = "Updated computer-use agent model for browser and desktop automation via screenshot input"
attachment = true
reasoning = false
tool_call = true
structured_output = true
temperature = true
release_date = "2026-05-09"
last_updated = "2026-05-09"
open_weights = false

[cost]
input = 0.50
output = 1.50

[limit]
context = 131_072
input = 131_072
output = 131_072

[modalities]
input = ["text", "image"]
output = ["text"]
