# BytePlus channel. Servable on GET /v1/models but not yet listed in the public
# pricing catalog, so [cost] is omitted rather than guessed.
name = "Seedance 2.5"
description = "Video model for prompt-guided generation, editing, and motion workflows"
family = "seed"
release_date = "2026-08-07"
last_updated = "2026-08-07"
attachment = false
reasoning = false
temperature = true
tool_call = false
open_weights = false

[limit]
context = 0
output = 0

[modalities]
input = ["text"]
output = ["video"]
