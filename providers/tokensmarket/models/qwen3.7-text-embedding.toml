# Alibaba Cloud channel. No lab entry exists for this model, so metadata is
# authored here. The catalog reports a 128K context and user-selectable vector
# dimensions of 256-2560, so `limit.output` records the maximum dimension.
# Embedding calls are input-billed only, so output cost is 0.
name = "Qwen3.7 Text Embedding"
description = "Multilingual embedding model for retrieval, clustering, classification, and code search"
family = "qwen"
release_date = "2026-05-21"
last_updated = "2026-05-21"
attachment = false
reasoning = false
temperature = false
tool_call = false
open_weights = false

[cost]
input = 0.0735
output = 0.00

[limit]
context = 128_000
output = 2_560

[modalities]
input = ["text"]
output = ["text"]
