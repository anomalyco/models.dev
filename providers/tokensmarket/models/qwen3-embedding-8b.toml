# SiliconFlow.cn channel. No lab entry exists for this model, so metadata is
# authored here. The catalog reports a 32K context and user-selectable vector
# dimensions of 32-4096, so `limit.output` records the maximum dimension.
# Embedding calls are input-billed only, so output cost is 0.
name = "Qwen3 Embedding 8B"
description = "Embedding model for semantic search, retrieval, clustering, and ranking pipelines"
family = "qwen"
release_date = "2025-06-05"
last_updated = "2025-06-05"
attachment = false
reasoning = false
temperature = false
tool_call = false
open_weights = true

[cost]
input = 0.04
output = 0.00

[limit]
context = 32_768
output = 4_096

[modalities]
input = ["text"]
output = ["text"]
