# Cost from https://klokintegration.se/docs/ai-api (email-token rate):
# €0.20 input / €1.00 output per MTok. EUR/USD 1.1583 (2026-08-28) → 0.23 / 1.16 USD.
# Limits are working values until the live API publishes them.
# Gateway appends Klok practice to the system prompt and runs lookup tools server-side.
name = "Kloker Integration Architect"
description = "Opinionated integration architect for the Klok ecosystem. The gateway injects Klok practice (data contracts, CloudEvents, event-driven flows) and runs ecosystem tools server-side. Client system prompts and OpenAI tool calls are preserved."
attachment = false
reasoning = false
tool_call = true
structured_output = false
temperature = true
release_date = "2026-08-29"
last_updated = "2026-08-29"
open_weights = false
status = "beta"

[cost]
input = 0.23
output = 1.16

[limit]
context = 128_000
output = 16_384

[modalities]
input = ["text"]
output = ["text"]
