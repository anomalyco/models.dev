# Cost: 25 SEK/MTok on klokintegration.se (indicative, no input/output split).
# Converted 2026-08-29 at 1 USD = 9.52 SEK (CurrencyBeacon, 2026-08-28) → 2.63 USD/MTok.
# Limits are unverified working values for agent use until the live API publishes them.
# Gateway appends Klok practice to the system prompt and runs lookup tools server-side.
name = "Kloker Integration Developer"
description = "Opinionated integration developer for the Klok ecosystem. The gateway injects Klok implementation practice and runs ecosystem tools server-side. Client system prompts and OpenAI tool calls are preserved."
attachment = false
reasoning = false
tool_call = true
structured_output = false
temperature = true
release_date = "2026-08-29"
last_updated = "2026-08-29"
open_weights = false
status = "beta"

[cost]
input = 2.63
output = 2.63

[limit]
context = 128_000
output = 16_384

[modalities]
input = ["text"]
output = ["text"]
