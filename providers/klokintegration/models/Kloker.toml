# Cost from https://klokintegration.se/docs/ai-api (email-token rate):
# €0.20 input / €1.00 output per MTok. EUR/USD 1.1583 (2026-08-28) → 0.23 / 1.16 USD.
# Limits are working values until the live API publishes them.
name = "Kloker"
description = "Cheap clean-context model. Nothing from the customer environment is packed in. It tracks the current best open source model; the Klok team verifies it and upgrades it periodically."
attachment = false
reasoning = false
tool_call = true
structured_output = false
temperature = true
release_date = "2026-08-29"
last_updated = "2026-08-29"
open_weights = false
status = "beta"

[cost]
input = 0.23
output = 1.16

[limit]
context = 128_000
output = 16_384

[modalities]
input = ["text"]
output = ["text"]
