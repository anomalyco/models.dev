# Image input verified on this host: a 32x32 solid-colour PNG as a data: URI was named correctly on
# 4 of 4 colour probes (probed 2026-09-17).
# No reasoning on this host: 54 requests returned 200 with no reasoning_content and a reasoning token
# count of 0. Alongside plain, top-level reasoning_effort (none, low, medium, high), reasoning.enabled,
# reasoning.effort, reasoning.max_tokens and thinking.type = "enabled" with budget_tokens, that includes
# Google's own controls in every shape the gateway might forward: thinking_budget with include_thoughts,
# thinking_config, google.thinking_config, and
# extra_body.google.thinking_config = { thinking_budget = 1024, include_thoughts = true }. All 36 of the
# recent requests were answered by Google AI Studio, and unlike the Gemini 3 ids on this host the route
# accepts reasoning_effort = "none" instead of rejecting it (probed 2026-09-17).
# Rates: GET https://api.cheaperinference.com/v1/models (pricing_version sha256:6d2520b9…, checked 2026-09-17T07:00:28Z)
# Discounted marketplace rate, 30.00% off the model maker's list price ($0.3/$2.5 per 1M).
base_model = "google/gemini-2.5-flash"
reasoning = false

[cost]
input = 0.21
output = 1.75
cache_read = 0.021
cache_write = 0.05831

[limit]
output = 65535
