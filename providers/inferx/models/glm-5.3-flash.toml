# Effort wire: {"reasoning_effort": "low"|"high"|"max"} — thinking cannot be disabled per Z.ai docs
base_model = "zhipuai/glm-5.3-flash"

[cost]
input = 0.075
output = 0.25
cache_read = 0.015

[[reasoning_options]]
type = "effort"
values = ["low", "high", "max"]
