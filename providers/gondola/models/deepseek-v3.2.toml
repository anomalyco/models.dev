# Cost = Gondola's live fee-inclusive "from" quote (/v1/models): the cheapest active
# lender's uniform pricing factor (a fraction of Venice list, applied to every model)
# + a 2% platform fee. So each model is the same multiple of Venice list right now;
# that multiple floats over time as supply shifts. Limits from /v1/models; modalities
# mirror providers/venice/. https://api.gondola-ai.com/v1/models (accessed 2026-07-24)
name = "DeepSeek V3.2"
description = "DeepSeek V3.2 served through the Gondola marketplace on Venice supply. Cost is a live fee-inclusive 'from' quote (a uniform multiple of Venice list plus the platform fee)."
family = "deepseek"
release_date = "2025-12-04"
last_updated = "2026-07-24"
attachment = false
reasoning = true
reasoning_options = []
temperature = true
tool_call = true
open_weights = true

[cost]
input = 0.158202
output = 0.230112

[limit]
context = 160000
output = 32768

[modalities]
input = ["text"]
output = ["text"]
