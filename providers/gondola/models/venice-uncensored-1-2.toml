# Cost = Gondola's live fee-inclusive "from" quote (/v1/models): the cheapest active
# lender's uniform pricing factor (a fraction of Venice list, applied to every model)
# + a 2% platform fee. So each model is the same multiple of Venice list right now;
# that multiple floats over time as supply shifts. Limits from /v1/models; modalities
# mirror providers/venice/. https://api.gondola-ai.com/v1/models (accessed 2026-07-24)
name = "Venice Uncensored 1.2"
description = "Venice Uncensored 1.2 served through the Gondola marketplace on Venice supply. Cost is a live fee-inclusive 'from' quote (a uniform multiple of Venice list plus the platform fee)."
family = "venice"
release_date = "2026-04-01"
last_updated = "2026-07-24"
attachment = true
reasoning = false
temperature = true
tool_call = true
open_weights = true

[cost]
input = 0.09588
output = 0.43146

[limit]
context = 128000
output = 8192

[modalities]
input = ["text", "image"]
output = ["text"]
