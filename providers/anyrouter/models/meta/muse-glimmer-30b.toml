# Host contract (GET /api/v1/models, accessed 2026-09-24): context_length=131072,
# input_modalities=[text, image], supported_parameters include tools/tool_choice.
# Params forwarded upstream (docs/features/provider-routing).
# Effort: reasoning_effort = low|medium|high|xhigh (OpenRouter peers)
base_model = "meta/muse-glimmer-30b"
reasoning_options = [{ type = "effort", values = ["low", "medium", "high", "xhigh"] }]

[cost]
input = 0.35
output = 1.5
