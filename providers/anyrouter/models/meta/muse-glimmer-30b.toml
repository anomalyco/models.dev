# Source: https://anyrouter.dev/api/v1/models (accessed 2026-09-24)
# Params are forwarded upstream (https://anyrouter.dev/docs/features/provider-routing);
# Effort: reasoning_effort = low|medium|high|xhigh (OpenRouter peers)
base_model = "meta/muse-glimmer-30b"
reasoning_options = [{ type = "effort", values = ["low", "medium", "high", "xhigh"] }]

[cost]
input = 0.35
output = 1.5
