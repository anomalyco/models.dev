# Host contract (GET /api/v1/models, accessed 2026-09-24): context_length=1000000,
# input_modalities=[text] (text-only route), supported_parameters=
# [max_tokens, temperature, top_p, stop, stream]. Flat USD/MTok — AnyRouter does
# not apply the lab's >200k context tier.
# Effort: reasoning_effort = none|low|medium|high (xAI + OpenRouter peers)
base_model = "xai/grok-4.3"
attachment = false
reasoning_options = [{ type = "effort", values = ["none", "low", "medium", "high"] }]

[cost]
input = 1.25
output = 2.5

[modalities]
input = ["text"]
