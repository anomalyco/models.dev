# Host contract (GET /api/v1/models, accessed 2026-09-24): context_length=1000000,
# input_modalities=[text]. Params forwarded upstream
# (docs/features/provider-routing).
# Toggle: reasoning.enabled = true|false
# Effort: reasoning_effort = medium|high (NVIDIA lab + OpenRouter peers)
base_model = "nvidia/nemotron-3-ultra-550b-a55b"
reasoning_options = [
  { type = "toggle" },
  { type = "effort", values = ["medium", "high"] },
]

[cost]
input = 0.5
output = 2.5
