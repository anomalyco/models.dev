# Host contract (GET /api/v1/models, accessed 2026-09-24): context_length=1048576,
# input_modalities=[text, image]. Params forwarded upstream
# (docs/features/provider-routing).
# Toggle: reasoning.enabled = true|false
# Effort: reasoning_effort = low|high|max (DeepSeek lab + OpenRouter peers)
base_model = "deepseek/deepseek-v4.1-flash"
reasoning_options = [
  { type = "toggle" },
  { type = "effort", values = ["low", "high", "max"] },
]

[cost]
input = 0.155
output = 0.62

[limit]
context = 1_048_576
