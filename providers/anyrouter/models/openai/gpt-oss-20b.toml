# Host contract (GET /api/v1/models, accessed 2026-09-24): context_length=131072,
# input_modalities=[text]. Params forwarded upstream
# (docs/features/provider-routing).
# Effort: reasoning_effort = low|medium|high (OpenRouter peers)
base_model = "openai/gpt-oss-20b"
reasoning_options = [{ type = "effort", values = ["low", "medium", "high"] }]

[cost]
input = 0.03
output = 0.14
