# Host contract (GET /api/v1/models, accessed 2026-09-24): context_length=200000,
# input_modalities=[text]. Params forwarded upstream
# (docs/features/provider-routing).
# Toggle: reasoning.enabled = true|false (Z.ai lab + OpenRouter peers)
base_model = "zhipuai/glm-4.7-flash"
reasoning_options = [{ type = "toggle" }]

[cost]
input = 0.06
output = 0.4
