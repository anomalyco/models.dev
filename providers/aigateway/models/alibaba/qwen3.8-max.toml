# Toggle: enable_thinking boolean, sent on every request (Alibaba DashScope
# OpenAI-compat dialect; verified against the live API).
# Effort: reasoning_effort = low|medium|xhigh.
base_model = "alibaba/qwen3.8-max"

[cost]
input = 2
output = 6

[[reasoning_options]]
type = "toggle"

[[reasoning_options]]
type = "effort"
values = ["low", "medium", "xhigh"]
