# Toggle: chat_template_kwargs.enable_thinking = true|false (default on)
# No reasoning tokens were observed on this host under any setting, so the
# toggle is carried from first-party z.ai rather than verified here.
base_model = "zhipuai/glm-5v-turbo"

[[reasoning_options]]
type = "toggle"

[interleaved]
field = "reasoning_content"

[cost]
input = 1.2
output = 4
cache_read = 0.3
cache_write = 1.5

[limit]
context = 202752
