# Toggle: chat_template_kwargs.enable_thinking = true|false
# Documented for this model by Workers AI, another OpenAI-compatible host:
# https://developers.cloudflare.com/workers-ai/models/nemotron-3-120b-a12b/sync-input.json
# It is also the field every GLM model on this host uses. Not reachable with the
# sync key (403, models=['public']), so it could not be confirmed here; the
# control shape follows first-party providers/nvidia/models/nvidia/
# nemotron-3-super-120b-a12b.toml. [interleaved] is omitted rather than assumed.
base_model = "nvidia/nemotron-3-super-120b-a12b"

[[reasoning_options]]
type = "toggle"

[cost]
input = 0.3
output = 0.9
cache_read = 0.075
cache_write = 0.375
