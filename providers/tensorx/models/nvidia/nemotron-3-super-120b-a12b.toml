# Not reachable with the sync key (403, models=['public']), so nothing here
# could be measured against this host. First-party providers/nvidia/models/
# nvidia/nemotron-3-super-120b-a12b.toml authors a toggle, so that control
# shape is copied rather than keeping a full effort enum.
# Toggle: wire path is neither documented by the lab entry nor verifiable with
# this key — confirm it before relying on it.
# [interleaved] is omitted rather than assumed, for the same reason.
base_model = "nvidia/nemotron-3-super-120b-a12b"

[[reasoning_options]]
type = "toggle"

[cost]
input = 0.3
output = 0.9
cache_read = 0.075
cache_write = 0.375
