# Not reachable with the sync key (403, models=['public']), so nothing below
# could be measured against this host. Controls are carried from the lab/peer
# baseline; [interleaved] is deliberately omitted rather than assumed, even
# though all 18 reachable models here stream reasoning_content.
# No lab or peer entry authors reasoning options, so the list is left as-is.
name = "DeepSeek Chat V3.1"
description = "DeepSeek chat model for instruction following, coding, and analysis"
family = "deepseek"
release_date = "2025-08-21"
last_updated = "2025-08-21"
attachment = false
reasoning = true
temperature = true
tool_call = true
knowledge = "2024-11"
open_weights = true

[[reasoning_options]]
type = "effort"
values = ["none", "minimal", "low", "medium", "high", "xhigh", "max"]

[cost]
input = 0.2
output = 0.8
cache_read = 0.05
cache_write = 0.25

[limit]
context = 164000
output = 163840

[modalities]
input = ["text"]
output = ["text"]
