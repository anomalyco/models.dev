# Pricing and context length: https://onlist.io/models/z-ai/glm-4.7 (accessed 2026-09-07)
# Toggle: top-level `thinking` = { type: "enabled"|"disabled" } (Z.AI field, not OpenRouter's `reasoning.enabled`)
base_model = "zhipuai/glm-4.7"

[[reasoning_options]]
type = "toggle"

[interleaved]
field = "reasoning_details"

[cost]
input = 0.4
output = 1.75
cache_read = 0.08

[limit]
context = 202_752
