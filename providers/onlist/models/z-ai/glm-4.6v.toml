# Pricing and context length: https://onlist.io/models/z-ai/glm-4.6v (accessed 2026-09-07)
# Toggle: top-level `thinking` = { type: "enabled"|"disabled" } (Z.AI field, not OpenRouter's `reasoning.enabled`)
base_model = "zhipuai/glm-4.6v"

[[reasoning_options]]
type = "toggle"

[cost]
input = 0.3
output = 0.9
cache_read = 0.055

[limit]
context = 131_072
