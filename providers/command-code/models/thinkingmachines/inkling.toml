# Effort: reasoning_effort = none|low|medium|high|xhigh|max (off = none; graded
# levels per TM lab set; minimal not authored — no host evidence it is accepted).
base_model = "thinkingmachines/inkling"
reasoning_options = [{ type = "effort", values = ["none", "low", "medium", "high", "xhigh", "max"] }]

[cost]
input = 1
output = 4.05
cache_read = 0.17

[limit]
context = 256_000
output = 256_000
