# Effort: reasoning_effort = high|max (low|medium map to high, xhigh to max)
base_model = "zhipuai/glm-5.2-fast"
reasoning_options = [{ type = "effort", values = ["high", "max"] }]

[cost]
input = 3
output = 10.25
cache_read = 0.5
