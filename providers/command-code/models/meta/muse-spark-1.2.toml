# Effort: reasoning_effort = low|medium|high|xhigh|max (live-probed 2026-08-14; host 400s minimal).
base_model = "meta/muse-spark-1.2"
reasoning_options = [{ type = "effort", values = ["low", "medium", "high", "xhigh", "max"] }]

[cost]
input = 1.25
output = 4.25
cache_read = 0.15

[modalities]
input = ["text", "image"]
