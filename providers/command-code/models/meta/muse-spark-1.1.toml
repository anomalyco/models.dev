# Effort: reasoning_effort = low|medium|high|xhigh (lab baseline; minimal 400-rejected; plan-gated from graded-effect probing).
# Served context from GET /provider/v1/models (accessed 2026-08-13): 1_048_576.
base_model = "meta/muse-spark-1.1"
reasoning_options = [{ type = "effort", values = ["low", "medium", "high", "xhigh"] }]

[cost]
input = 1.25
output = 4.25
cache_read = 0.15

[limit]
context = 1_048_576

[modalities]
input = ["text", "image"]
