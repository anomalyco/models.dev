# Effort: reasoning_effort = low|medium|high|xhigh|max (live-probed 2026-08-14: all five accepted;
# enable_thinking / thinking_budget accepted but unreliable — not authored).
base_model = "alibaba/qwen3.8-max"
name = "Qwen 3.8 Max"
reasoning_options = [{ type = "effort", values = ["low", "medium", "high", "xhigh", "max"] }]
interleaved = { field = "reasoning_content" }

[cost]
input = 2
output = 6
cache_read = 0.25
cache_write = 2.5

[modalities]
input = ["text", "image"]
