# Price mirrors Google's launch discount (50% off list, ends 2026-12-31).
# Effort: reasoning_effort = low|medium|high (lab baseline; xhigh|max not graded-probed — plan-gated).
# Served context from GET /provider/v1/models: 1_048_576 (probed 2026-08-14).
base_model = "google/gemini-3.7-flash"
reasoning_options = [{ type = "effort", values = ["low", "medium", "high"] }]

[cost]
input = 0.75
output = 3.75
cache_read = 0.075

[modalities]
input = ["text", "image"]
