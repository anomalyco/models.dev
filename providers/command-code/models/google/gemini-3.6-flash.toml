# Effort: reasoning_effort = low|medium|high (lab baseline; minimal 400-rejected by gateway; xhigh|max not probed — plan-gated).
base_model = "google/gemini-3.6-flash"
reasoning_options = [{ type = "effort", values = ["low", "medium", "high"] }]

[cost]
input = 1.5
output = 7.5
cache_read = 0.15

[limit]
context = 1_000_000

[modalities]
input = ["text", "image"]
