# Toggle: thinking.type = enabled|disabled (live-verified 2026-08-14: rtok 181→18 when disabled).
# Effort: reasoning_effort = low|medium|high|xhigh|max — beyond the lab set, but every level
# measured distinct on this host (rtok 66/275/139/513/600, 2026-08-14).
# Emits reasoning via reasoning / reasoning_details.
base_model = "deepseek/deepseek-v4-flash"
name = "DeepSeek V4 Flash (latest)"
reasoning_options = [{ type = "toggle" }, { type = "effort", values = ["low", "medium", "high", "xhigh", "max"] }]
interleaved = { field = "reasoning_details" }

[cost]
input = 0.14
output = 0.28
cache_read = 0.0028
