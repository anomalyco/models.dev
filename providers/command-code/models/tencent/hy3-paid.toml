# Effort: reasoning_effort = low|medium|high|xhigh|max (live-probed 2026-08-14;
# chat_template_kwargs.reasoning_effort = no_think did NOT suppress reasoning — falsified).
# Emits reasoning via reasoning / reasoning_details.
base_model = "tencent/hy3"
name = "Tencent Hy3"
reasoning_options = [{ type = "effort", values = ["low", "medium", "high", "xhigh", "max"] }]
interleaved = { field = "reasoning_details" }

[cost]
input = 0.14
output = 0.58
cache_read = 0.035

[limit]
context = 262_144
