# Prices: https://commandcode.ai/docs/resources/pricing-limits (accessed 2026-09-01).
# Catalog/ids: https://api.commandcode.ai/provider/v1 and https://commandcode.ai/models (accessed 2026-09-01).
# Relay notes: OpenAI Chat Completions on /provider/v1/chat/completions; reasoning controls
# pass through to upstreams (mirrored from the labs' own entries).
# Toggle: thinking.type = enabled|disabled (default disabled)
# Effort: reasoning_effort = none|high
# Host context 1048576 per the Command Code catalog/API context_length (lab entry: 1024000).

base_model = "tencent/hy4-preview"
name = "Tencent Hy4 Preview"

reasoning_options = [{ type = "toggle" }, { type = "effort", values = ["none", "high"] }]

[cost]
input = 0.834
output = 2.501
cache_read = 0.042

[limit]
context = 1048576
