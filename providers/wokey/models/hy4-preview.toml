# Sources (accessed 2026-09-25):
# - https://api.wokey.ai/v1/models/pricing (public, no key; id "hy4-preview"; pricing_mode dynamic_discount)
# - https://api.wokey.ai/v1/models (context_length, max_completion_tokens, input_modalities)
# - https://wokey.ai/models/hy4-preview
# Reasoning options copied from providers/tencent-tokenhub/models/hy4-preview.toml (Wokey relays the upstream API).
# Toggle: thinking.type = enabled|disabled
# Effort: reasoning_effort = none|high
# Off is effort=none, so no separate toggle option (AGENTS.md reasoning options).
base_model = "tencent/hy4-preview"

[[reasoning_options]]
type = "effort"
values = ["none", "high"]

[cost]
input = 0.417
output = 1.2505
cache_read = 0.021
