# Sources (accessed 2026-09-25):
# - https://api.wokey.ai/v1/models/pricing (public, no key; id "glm-5.3-flash"; pricing_mode dynamic_discount)
# - https://api.wokey.ai/v1/models (context_length, max_completion_tokens, input_modalities)
# - https://wokey.ai/models/glm-5.3-flash
# Reasoning options copied from providers/zhipuai/models/glm-5.3-flash.toml (Wokey relays the upstream API).
# Input modalities narrowed to the list Wokey publishes for this model.
base_model = "zhipuai/glm-5.3-flash"

[[reasoning_options]]
type = "effort"
values = ["low", "high", "max"]

[cost]
input = 0.075
output = 0.25
cache_read = 0.015

[modalities]
input = ["text", "image"]
